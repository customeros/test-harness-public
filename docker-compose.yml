version: '3.8'

services:
  app:
    build:
      context: .github
      dockerfile: Dockerfile
    container_name: react-docker-app
    ports:
      - "3000:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    environment:
      - NODE_ENV=production